# Astro
dist/
.astro/

# Node
node_modules/
npm-debug.log*

# npm workspaces ではルートの package-lock.json に集約するため、
# サブパッケージの lock は追跡しない（--prefix での install 時に生成されることがある）
package-lock.json

# 環境変数（apps/web 固有がある場合）
.env
.env.local

# Playwright
test-results/
playwright-report/
.playwright/

# Lighthouse CI
.lighthouseci/

# Vitest
coverage/

# OS / IDE（ルートでも除外しているが念のため）
.DS_Store
Thumbs.db
